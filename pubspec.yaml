name: flutter_sample_shell_route
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.3.3
  pedantic_mono: ^1.21.1

flutter:
  uses-material-design: true
